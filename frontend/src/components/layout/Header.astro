---
import Nav from './Nav.astro'
const base = import.meta.env.BASE_URL
---

<header class="site-header">
  <div class="container site-header-inner">
    <a href={base} class="site-logo" aria-label="MOSAIC — home">
      <img
        src={`${base}/mosaic-mark-reverse.svg`}
        alt=""
        class="site-logo-mark"
        width="40"
        height="26"
        aria-hidden="true"
      />
      <div class="site-logo-text">
        <span class="site-logo-name">MOSAIC</span>
        <span class="site-logo-tagline">CGIAR MFL Science Programme</span>
      </div>
    </a>

    <Nav />
  </div>
</header>

<style>
  /* Teal header — matches the footer (var(--mosaic-teal), Ocean Teal) so the
     two MOSAIC "bookends" of the page share the same colour, in both themes. */
  .site-header {
    background-color: var(--color-bookend);
    border-bottom: 3px solid var(--mosaic-teal-dark);
    position: sticky;
    top: 0;
    z-index: 100;
    box-shadow: 0 1px 12px rgba(0, 0, 0, 0.18);
  }

  .site-header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: var(--space-3);
    padding-bottom: var(--space-3);
    gap: var(--space-4);
  }

  .site-logo {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    text-decoration: none;
    color: var(--color-white);
    flex-shrink: 0;
  }

  .site-logo:focus-visible {
    outline: 2px solid var(--mosaic-teal);
    outline-offset: 3px;
    border-radius: var(--radius-sm);
  }

  .site-logo-mark {
    height: 26px;
    width: auto;
    flex-shrink: 0;
  }

  .site-logo-text {
    display: flex;
    flex-direction: column;
    line-height: 1.15;
    gap: 1px;
  }

  .site-logo-name {
    font-family: var(--font-title);
    font-size: var(--text-xl);
    font-weight: 700;
    letter-spacing: 0.04em;
    color: var(--color-white);
  }

  .site-logo-tagline {
    font-size: 0.65rem;
    color: var(--mosaic-light-teal);
    letter-spacing: 0.07em;
    text-transform: uppercase;
    font-weight: 600;
  }

  @media (max-width: 480px) {
    .site-logo-tagline { display: none; }
  }
</style>
