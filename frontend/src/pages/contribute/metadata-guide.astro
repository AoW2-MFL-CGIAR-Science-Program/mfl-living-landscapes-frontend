---
import BaseLayout from '../../layouts/BaseLayout.astro'
import PageHero from '../../components/ui/PageHero.astro'
import Breadcrumb from '../../components/ui/Breadcrumb.astro'
const base = import.meta.env.BASE_URL
---

<BaseLayout title="Metadata Guide" description="Complete guide to all metadata fields required for MOSAIC.">
  <PageHero title="Metadata Guide" subtitle="Every field explained — what it means, what values are accepted, and what a good entry looks like." />
  <div class="container">
    <Breadcrumb crumbs={[
      { label: 'Home', href: `${base}/` },
      { label: 'Contribute', href: `${base}/contribute` },
      { label: 'Metadata Guide' },
    ]} />
  </div>

  <div class="container" style="padding-top: var(--space-12); padding-bottom: var(--space-16); max-width: 900px;">

    <section style="margin-bottom: var(--space-12);">
      <h2 style="margin-bottom: var(--space-6);">Required fields</h2>
      <table class="data-table">
        <thead>
          <tr>
            <th>Field</th>
            <th>Type</th>
            <th>Description</th>
            <th>Example</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code>id</code></td>
            <td>String</td>
            <td>Unique identifier. Format: LANDSCAPE_THEME-CODE_YEAR_SOURCE_VERSION</td>
            <td><code>KEN-LVB_LC_2023_ILRI_v1</code></td>
          </tr>
          <tr>
            <td><code>title</code></td>
            <td>String</td>
            <td>Human-readable dataset name</td>
            <td>Lake Victoria Basin Land Cover 2023</td>
          </tr>
          <tr>
            <td><code>country</code></td>
            <td>Controlled vocabulary</td>
            <td>Country where the landscape is located</td>
            <td>Kenya</td>
          </tr>
          <tr>
            <td><code>living_landscape</code></td>
            <td>Controlled vocabulary</td>
            <td>Living Landscape code</td>
            <td>KEN-LVB</td>
          </tr>
          <tr>
            <td><code>mfl_theme</code></td>
            <td>Controlled vocabulary</td>
            <td>The MFL minimum spatial package theme this dataset addresses</td>
            <td>Land cover / land use</td>
          </tr>
          <tr>
            <td><code>data_type</code></td>
            <td>Controlled vocabulary</td>
            <td>Technical format of the dataset</td>
            <td>Raster</td>
          </tr>
          <tr>
            <td><code>access_level</code></td>
            <td>Controlled vocabulary</td>
            <td>Who can access this dataset</td>
            <td>Open</td>
          </tr>
          <tr>
            <td><code>license</code></td>
            <td>String</td>
            <td>License under which the dataset is shared. Must not be empty.</td>
            <td>CC BY 4.0</td>
          </tr>
          <tr>
            <td><code>readiness_status</code></td>
            <td>Controlled vocabulary</td>
            <td>How far along the QC process this dataset is</td>
            <td>Validated</td>
          </tr>
        </tbody>
      </table>
    </section>

    <section style="margin-bottom: var(--space-12);">
      <h2 style="margin-bottom: var(--space-6);">Optional fields</h2>
      <table class="data-table">
        <thead>
          <tr>
            <th>Field</th>
            <th>Type</th>
            <th>Notes</th>
            <th>Example</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code>description</code></td>
            <td>String</td>
            <td>1–3 sentence plain-language summary of what the dataset contains</td>
            <td>Land cover classification for the Kenya portion of the Lake Victoria Basin...</td>
          </tr>
          <tr>
            <td><code>source</code></td>
            <td>String</td>
            <td>Contributing CGIAR centre or organisation</td>
            <td>ILRI</td>
          </tr>
          <tr>
            <td><code>contact</code></td>
            <td>Email</td>
            <td>Contact email for access requests or questions</td>
            <td>a.paliwal@cgiar.org</td>
          </tr>
          <tr>
            <td><code>spatial_resolution</code></td>
            <td>String</td>
            <td>Pixel size or feature resolution</td>
            <td>10 m</td>
          </tr>
          <tr>
            <td><code>temporal_coverage</code></td>
            <td>String</td>
            <td>Year or date range the dataset covers</td>
            <td>2023 or 2020–2023</td>
          </tr>
          <tr>
            <td><code>download_url</code></td>
            <td>URL</td>
            <td>Required when access_level is Open. Direct link to download or data portal.</td>
            <td>https://data.ilri.org/portal/dataset/ken-lv-lc-2023</td>
          </tr>
          <tr>
            <td><code>metadata_url</code></td>
            <td>URL</td>
            <td>Link to full metadata record (GeoNetwork, CKAN, institutional portal)</td>
            <td>https://data.ilri.org/portal/dataset/ken-lv-lc-2023</td>
          </tr>
        </tbody>
      </table>
    </section>

    <section style="margin-bottom: var(--space-12);">
      <h2 style="margin-bottom: var(--space-6);">Controlled vocabulary tables</h2>

      <h3 style="margin-bottom: var(--space-3);">Country</h3>
      <p style="color: var(--color-gray-600); margin-bottom: var(--space-3); font-size: var(--text-sm);">Priority countries for 2026: Kenya, Ethiopia, India.</p>
      <div style="display: flex; flex-wrap: wrap; gap: var(--space-2); margin-bottom: var(--space-8);">
        {['Kenya', 'Ethiopia', 'India', 'Colombia', 'Peru', "Côte d'Ivoire", 'Senegal', 'Tanzania', 'Tunisia', 'Vietnam', 'Laos', 'Cambodia', 'Zimbabwe'].map((v) => (
          <code style="background-color: var(--color-gray-100); padding: var(--space-1) var(--space-2); border-radius: var(--radius-sm); font-size: var(--text-sm);">{v}</code>
        ))}
      </div>

      <h3 style="margin-bottom: var(--space-3);">Living Landscape</h3>
      <p style="color: var(--color-gray-600); margin-bottom: var(--space-3); font-size: var(--text-sm);">
        These are the <strong>canonical</strong> MFL Living Landscape codes (approved 2026-07-21),
        matching the delineated landscape boundaries.
      </p>
      <table class="data-table" style="margin-bottom: var(--space-4);">
        <thead><tr><th>Code</th><th>Landscape Name</th><th>Country</th></tr></thead>
        <tbody>
          {[
            ['COL-CUM', 'Cumbal (Nariño)', 'Colombia'],
            ['PER-PCL', 'Pucallpa – Ucayali', 'Peru'],
            ['SEN-FK', 'Fatick Department', 'Senegal'],
            ['CIV-NZ', "N'Zi Watershed", "Côte d'Ivoire"],
            ['TUN-NW', 'Northwestern Tunisia', 'Tunisia'],
            ['ETH-OG', 'Omo-Gibe Basin', 'Ethiopia'],
            ['KEN-LVB', 'Lake Victoria Basin (Kenya)', 'Kenya'],
            ['KEN-LEI', 'LEILA (Southern Kenya Rangelands)', 'Kenya'],
            ['ZWE-MB', 'Mbire – Lower Zambezi Valley', 'Zimbabwe'],
            ['IND-CH', 'Central India Highlands', 'India'],
            ['MEK-3S', '3S Basins (Sekong, Sesan, Srepok)', 'Laos / Cambodia / Vietnam'],
          ].map(([code, name, country]) => (
            <tr><td><code>{code}</code></td><td>{name}</td><td>{country}</td></tr>
          ))}
        </tbody>
      </table>
      <p style="color: var(--color-gray-600); margin-bottom: var(--space-8); font-size: var(--text-sm);">
        Datasets that are wider than a single landscape use the registry values
        <strong>"NATIONAL — Country-wide coverage"</strong> or
        <strong>"GLOBAL — Global / cross-landscape"</strong>. In the published catalogue these
        resolve to the country's landscape collection or to the special value <code>GLB</code>
        respectively, and each record carries a <code>coverage</code> field
        (<code>landscape</code> | <code>national</code> | <code>global</code>) recording the
        dataset's true footprint.
      </p>

      <h3 style="margin-bottom: var(--space-3);">MFL Theme</h3>
      <div style="display: flex; flex-wrap: wrap; gap: var(--space-2); margin-bottom: var(--space-8);">
        {['Boundaries / admin units','Land cover / land use','Ecosystem condition','Degradation / land health','Water / hydrology','Biodiversity / ecosystems','Pressures / drivers','Ecosystem services','Hotspots / leverage points','Scenarios / future risks','Decision-support outputs','Agrobiodiversity / crops','Socio-economic / livelihoods'].map((v) => (
          <code style="background-color: var(--color-gray-100); padding: var(--space-1) var(--space-2); border-radius: var(--radius-sm); font-size: var(--text-sm);">{v}</code>
        ))}
      </div>

      <h3 style="margin-bottom: var(--space-3);">Data Type</h3>
      <div style="display: flex; flex-wrap: wrap; gap: var(--space-2); margin-bottom: var(--space-8);">
        {['Raster','Vector','Tabular','Mixed'].map((v) => (
          <code style="background-color: var(--color-gray-100); padding: var(--space-1) var(--space-2); border-radius: var(--radius-sm); font-size: var(--text-sm);">{v}</code>
        ))}
      </div>

      <h3 style="margin-bottom: var(--space-3);">Access Level</h3>
      <p style="color: var(--color-gray-600); margin-bottom: var(--space-3); font-size: var(--text-sm);">
        Access level decisions are made by the contributing centre, not by the MOSAIC team. MOSAIC records and displays the access level the contributor declares, and never overrides it.
      </p>
      <table class="data-table" style="margin-bottom: var(--space-8);">
        <thead><tr><th>Value</th><th>Who can access</th><th>How to access</th></tr></thead>
        <tbody>
          <tr><td><span class="badge badge-open">Open</span></td><td>Anyone — the dataset is publicly available</td><td>Direct download link on the dataset detail page (a <code>download_url</code> is required)</td></tr>
          <tr><td><span class="badge badge-restricted">Restricted</span></td><td>Approved users — access is granted by the contributing centre</td><td>Contact email shown on the dataset detail page; decision made by the data owner</td></tr>
          <tr><td><span class="badge badge-internal">Internal</span></td><td>CGIAR staff only — for datasets not ready for external sharing</td><td>Contact the MOSAIC team; verification of CGIAR affiliation required</td></tr>
        </tbody>
      </table>

      <h3 style="margin-bottom: var(--space-3);">Readiness Status</h3>
      <table class="data-table">
        <thead><tr><th>Status</th><th>Meaning</th></tr></thead>
        <tbody>
          <tr><td><span class="badge badge-raw">Raw</span></td><td>Registered as supplied by the source; not yet processed or quality-checked. Metadata may be incomplete.</td></tr>
          <tr><td><span class="badge badge-processed">Processed</span></td><td>Cleaned, reformatted, or harmonized, but not yet fully validated against the MFL spatial standards.</td></tr>
          <tr><td><span class="badge badge-validated">Validated</span></td><td>Passed quality control and meets the MFL spatial standards. Suitable for analysis.</td></tr>
        </tbody>
      </table>
    </section>

  </div>
</BaseLayout>

<style>
  @import '../../styles/catalogue.css';
</style>
